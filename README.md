# auto-forex-notebooks

JupyterLab environment for AutoForexV2 exploration and analysis.

## Setup

```bash
uv sync
uv run jupyter lab
```

Depends on the `core`, `server`, `snowball`, and `oanda` packages via local
path sources.
